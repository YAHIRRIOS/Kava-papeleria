<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/login.css" rel="stylesheet">
    <title>Registro Farmacia</title>
</head>
<body>
    <% var mensaje = typeof mensaje !== 'undefined' ? mensaje : null; %>
    <% if (mensaje) { %>
        <script>
            alert('<%= mensaje %>');
        </script>
    <% } %>
    <div class="login-container">
        <div class="login-text">
            <h2>Ingresa los datos para crear una cuenta</h2>
        </div>
      
        <div class="login-form">
            <form action="/usuarios/register" method="POST">
                <div class="input-group">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" placeholder="E-mail" required>
                </div>
                <div class="input-group">
                    <label for="password">Ingresa una Contraseña</label>
                    <input type="password" id="password" name="password" placeholder="Contraseña" required>
                </div>
                <button type="submit" class="login-btn">Continuar</button>
            </form>
            <div class="register-link"><a href="/usuarios/login">Ya tienes cuenta? Incia Sesion</a></div>
        </div>
    </div>
</body>
</html>
